# Copy this file to .env and fill in your values

# Set this to your Spring Boot backend URL to enable backend mode.
# Without this, the app runs in localStorage demo mode.
# VITE_API_URL=http://localhost:8080

# Leave blank for localStorage demo mode (no backend required):
VITE_API_URL=
